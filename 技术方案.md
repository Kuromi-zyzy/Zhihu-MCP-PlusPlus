# ZhihuSpider 技术方案

> ⚠️ **历史设计参考**：部分内容（特别是"二、Cookie 自动捕获"中的 `page.listen.start` 监听方案）与现行代码不符。`login.py` 现采用 `page.cookies()` 直接读取。**一切以代码为准**，本文档仅供架构思路参考。

> Cookie 自动捕获 + API 爬虫的完整实现参考
> 下次新项目需要类似功能时，直接复制这里的模式。

---

## 一、整体架构

```
┌─────────────────────────────────────────────────┐
│                   main.py                        │
│  入口调度、CLI 参数、Cookie 优先级合并            │
└────────────┬────────────────────────┬────────────┘
             │                        │
    ┌────────▼────────┐     ┌─────────▼─────────┐
    │   login.py       │     │  crawler.py        │
    │ DrissionPage     │     │ requests + API     │
    │ 浏览器控制       │     │ 网络请求 + 重试    │
    │ 监听网络请求     │     │ 分页遍历           │
    │ 提取 Cookie      │     │                    │
    └────────┬─────────┘     └─────────┬──────────┘
             │                        │
             │              ┌─────────▼──────────┐
             │              │   parser.py         │
             │              │ BeautifulSoup       │
             │              │ HTML → Markdown     │
             │              │ 元数据提取          │
             │              └─────────┬──────────┘
             │                        │
             └──────────┬─────────────┘
                        ▼
            ┌─────────────────────┐
            │   zhihu_spider.py    │
            │ 业务编排、文件保存   │
            └─────────────────────┘
```

### 核心原则

- **API 优先，HTML 兜底**：优先用网站 API（JSON），结构稳定、解析简单
- **Cookie 自动捕获**：用户无感，打开浏览器登录即可，不手动复制
- **职责分离**：网络请求、内容解析、业务逻辑分成独立模块

---

## 二、Cookie 自动捕获（关键模块）

### 技术选型：DrissionPage

- 对比 Selenium：API 更简洁、速度更快、无需 WebDriver
- 对比手动复制：用户体验好，登录后自动抓取

### 核心逻辑

```python
# 1. 打开浏览器
page = ChromiumPage()

# 2. 启动网络监听（指定要监听的 URL 关键字）
page.listen.start("zhihu.com/api/v4")       # 监听目标 API

# 3. 打开登录页
page.get("https://www.zhihu.com/signin?next=%2F")

# 4. 等待用户登录完成（监听捕获到一个 API 请求）
packet = page.listen.wait(timeout=120)

# 5. 从请求头中提取 Cookie
cookie_str = packet.request.headers.get("Cookie", "")
```

### 可复用模式

对于**其他网站**的 Cookie 捕获，只需改 3 个参数：

| 参数 | 说明 | 示例 |
|------|------|------|
| `LOGIN_URL` | 网站的登录页 URL | `https://weibo.com/login` |
| `LISTEN_TARGET` | 监听哪个 API 域名/路径 | `"api.weibo.com"` |
| `COOKIE_BLACKLIST` | 需要过滤掉的无关 Cookie key | `["Hm_lvt_", ...]` |

### Cookie 安全

- 存在本地 `config.json`（不入 git）
- 支持 `.env`、环境变量、CLI 参数多种注入方式
- 优先级：CLI > config.json > .env > 环境变量

---

## 三、API 爬虫（核心逻辑）

### 请求层设计

```python
class ZhihuCrawler:
    def __init__(self, cookie, proxies, timeout=15, retries=3):
        self.session = requests.Session()
        self.session.headers.update(DEFAULT_HEADERS)
        self.session.headers["Cookie"] = cookie  # 注入 Cookie
        # 支持代理
        if proxies:
            self.session.proxies.update(proxies)
```

### 请求模板方法

```python
def _request(self, url):
    for i in range(self.retries):          # 重试 3 次
        try:
            resp = self.session.get(url, timeout=self.timeout)
            if resp.status_code == 200:
                return resp
            elif resp.status_code == 403:
                print("被拦截，需检查 Cookie")
                return None
        except (Timeout, ConnectionError) as e:
            print(f"网络错误 (第{i+1}次)")
            time.sleep(2 * (i + 1))        # 指数退避
    return None
```

**新项目可复用此模式**：换 API URL + 换 headers 即可。

### 分页遍历器

```python
def get_answers(self, question_id):
    offset = 0
    while True:
        url = f"API_URL?limit=20&offset={offset}"
        resp = self._request(url)
        data = resp.json()
        items = data.get("data", [])
        if not items:
            break
        yield from items
        offset += len(items)
        if data.get("paging", {}).get("is_end"):
            break
        time.sleep(3)  # 控制频率
```

### 反爬应对

| 问题 | 方案 |
|------|------|
| 403 Forbidden | 浏览器 Cookie 认证（DrissionPage 捕获） |
| 请求频率限制 | `time.sleep(3)` 控制间隔 |
| IP 封禁 | 支持 `--proxy` 代理参数 |
| 连接超时 | 3 次重试 + 指数退避 |
| JSON 解析失败 | try/except 容错 |

---

## 四、内容解析（HTML → Markdown）

### 技术选型

- **BeautifulSoup**：解析 HTML，容错性好
- **手动递归转换**：而不是 html2text 库，控制力更强

### 核心模式：递归节点遍历

```python
def _parse_node(node):
    parts = []
    for child in node.children:
        if isinstance(child, str):
            parts.append(child.strip())
            continue
        tag = child.name
        if tag in ("h1", "h2", ...):
            parts.append(f"\n{'#' * level} {text}\n")
        elif tag == "p":
            parts.append(f"\n{_parse_inline(child)}\n")
        elif tag in ("ul", "ol"):
            # 处理列表
        elif tag == "a":
            href, text = child.get("href"), child.get_text()
            parts.append(f"[{text}]({href})")
        elif tag == "img":
            src = child.get("src")
            parts.append(f"\n![alt]({src})\n")
        # ... 其他标签
    return "".join(parts)
```

**新项目复用**：主要增加/修改需要处理的 HTML 标签即可。

---

## 五、元数据提取

每个 API 返回的内容中提取结构化的元数据：

```python
def extract_metadata(answer_data):
    return {
        "title":    answer_data["question"]["title"],
        "author":   answer_data["author"]["name"],
        "voteup":   answer_data["voteup_count"],
        "url":      f"https://zhihu.com/question/{qid}/answer/{aid}",
        "created":  timestamp_to_date(answer_data["created_time"]),
    }
```

保存为 Markdown 文件头部的 YAML front matter：

```markdown
---
title: xxx
author: xxx
voteup: 1234
url: https://...
---
```

---

## 六、项目结构模板（新项目直接复制）

```
YourNewProject/
├── main.py              # 入口 + CLI 参数
├── login.py             # DrissionPage Cookie 自动捕获
├── crawler.py           # API 请求层
├── parser.py            # HTML → Markdown 解析
├── spider.py            # 业务编排 + 文件保存
├── config.json          # Cookie 存储（自动生成，不入 git）
├── config.example.json  # 配置示例
├── .gitignore           # 忽略 config.json、output/
├── requirements.txt     # 依赖清单
└── start.bat            # 一键启动（Windows）
```

### 依赖清单

```
requests>=2.28.0        # HTTP 请求
beautifulsoup4>=4.12.0  # HTML 解析
lxml>=4.9.0             # BeautifulSoup 加速解析器
DrissionPage>=4.0.0     # 浏览器自动化，捕获 Cookie
```

---

## 七、适配其他网站 checklist

1. 确定目标网站 API 端点（F12 → Network 观察）
2. 修改 `login.py` 中的 LOGIN_URL 和 LISTEN_TARGET
3. 修改 `crawler.py` 中的 API URL 和 headers
4. 修改 `parser.py` 中的 HTML 标签处理规则
5. 修改元数据提取字段
6. 测试 Cookie 捕获 + 数据爬取

---

## 八、局限性

| 限制 | 说明 |
|------|------|
| API 变更 | 网站改版后 API 可能变化，需更新 URL |
| DrissionPage 兼容性 | 依赖 Chrome/Edge 浏览器 |
| 仅支持文本内容 | 图片、视频等多媒体只保留链接 |
| 法律风险 | 大量爬取请遵守 robots.txt 和相关法规 |
